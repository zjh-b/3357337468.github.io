export const meta = {
  name: 'paper-patterns',
  description: '范文模式库：从国赛优秀论文中提取摘要/公式/论证/创新模式，注入论文生成流程',
  phases: [
    { title: '模式提取', detail: '从范文和算法指南中提取可复用模式' },
    { title: '摘要模式', detail: '提取获奖摘要的结构模式' },
    { title: '论证模式', detail: '提取公式解释/灵敏度/过渡的模式' },
    { title: '算法匹配', detail: '基于算法指南的问题-算法智能匹配' },
    { title: '注入方案', detail: '生成可注入到paper-generate的prompt增强' },
  ],
}

// 范文模式库工作流
// 目标：从国赛优秀论文和算法指南中提取可复用的写作和建模模式，
//       注入到 paper-generate.js 的各阶段 prompt 中，提升论文质量
//
// 输入: args = { problemType: "优化类", contest: "cumcm" }
// 输出: 结构化的模式注入方案

const config = args || {}
const problemType = config.problemType || '优化类'
const contest = config.contest || 'cumcm'

log('========================================')
log('  范文模式库 — 从优秀论文中学习')
log('========================================')
log(`问题类型: ${problemType} | 竞赛: ${contest}`)
log('')

// ===== 第一阶段：多维度并行模式提取 =====
phase('模式提取')
log('正在从范文和算法指南中提取可复用模式...')

const [abstractPatterns, formulaPatterns, sensitivityPatterns, algorithmMatch, innovationPatterns] = await parallel([
  // 1. 摘要模式
  () => agent(
    `你是国赛优秀论文分析专家。请分析获奖论文摘要的共性模式。

## 背景
2020年国赛优秀论文（A-E题共17篇一等奖论文）展示了以下摘要模式：

## 请提取：获奖摘要的4种黄金结构

### 模式1：数字驱动型摘要
结构：具体数字开头 → 方法简述 → 关键结果(含数字) → 一句话启示
特征：第一句话就出现量化数字（如"XX问题影响每年超过2000万吨碳排放"）
适用场景：问题有明确的量化背景

### 模式2：问题洞察型摘要
结构：问题本质揭示 → 传统方法为何不足 → 本文方法 → 关键突破 → 启示
特征：以洞察开场而非背景描述（如"XX问题的本质不是路径规划，而是多目标约束下的资源博弈"）
适用场景：问题有反直觉特征或需要深度分析

### 模式3：方法创新型摘要
结构：问题简述 → 现有方法局限性 → 本文创新方法(重点描述) → 对比结果 → 推广价值
特征：重心放在方法创新上，详细说明创新点和效果
适用场景：算法/模型有明显创新

### 模式4：综合影响型摘要
结构：问题背景(1句) → 多子问题概述 → 方法框架 → 各子问题关键结果 → 综合启示
特征：适合多子问题综合题，每个子问题1-2个数字
适用场景：综合类问题（E题等多子问题）

## 对每种模式输出：
1. **模板框架**（可直接填充的摘要骨架）
2. **字数分配**（每个部分占多少字）
3. **数字密度要求**（至少几个数字）
4. **一个完整的优秀示例**（300-500字，虚构但符合获奖水平）

## 额外要求
- 提炼"获奖摘要的3条铁律"（如"前50字必须出现数字"、"最后一句话让评委记住你"）
- 提炼"摘要最常见的5个扣分点"`,
    { label: '摘要模式', phase: '模式提取' }
  ),

  // 2. 公式解释模式
  () => agent(
    `你是数学建模论文写作专家。请分析获奖论文中公式解释的共性模式。

## 背景
分析国赛一等奖论文后发现，公式解释深度是区分"省一"和"省二"的关键分水岭。

## 请提取：公式解释的黄金模式

### 当前通用模式（及格水平）
"其中，C_vehicle表示车辆启动成本，C_energy表示能耗成本..."

### 获奖论文模式（高分水平）
需要你构建"公式解释深度金字塔"：

#### 第1层：符号定义（基础分，必须做）
每个符号的数学定义和取值范围
示例："N={1,2,...,n}表示客户节点集合，K={1,2,...,m}表示可用车辆集合"

#### 第2层：物理/经济含义（加分项）
符号对应的实际物理或经济含义
示例："c_k^fixed表示第k辆车的固定启动成本，包括司机日薪(约300元)、车辆折旧(约150元)、保险分摊(约50元)，合计约500元/车次"

#### 第3层：优化倾向（高分项）
该项如何影响模型的决策方向
示例："C_vehicle项的存在使模型倾向于：(1)用尽量少的车辆；(2)每辆车尽量满载。这解释了为何优化结果中车辆数从贪心方案的18辆降至14辆——模型通过合并路线降低了固定成本"

#### 第4层：灵敏度/临界点（顶级论文）
该项在什么条件下成为主导因素
示例："当订单密度>50单/km²时，C_vehicle是成本的主要成分(>60%)；当订单密度<10单/km²时，C_energy超过C_vehicle。这解释了为何城区和郊区的配送策略应有所不同"

## 约束解释的升级模式
从"约束(1)确保容量不超载"升级为：
"约束(1)确保每辆车的装载量不超过额定载重。如果违反：(a)实际配送无法执行；(b)在小规模实例中，违反此约束的解与实际可行解的偏差达15-30%。我们通过惩罚函数处理软约束..."

## 输出每种模式的模板+示例`,
    { label: '公式解释模式', phase: '模式提取' }
  ),

  // 3. 灵敏度分析模式
  () => agent(
    `你是数学建模评审专家。请分析获奖论文中灵敏度分析的深层模式。

## 背景
国赛论文中，80%的灵敏度分析停留在"参数±10%，结果变化5%，影响不大"的水平。
而一等奖论文的灵敏度分析揭示了**非平凡关系**。

## 请提取：灵敏度分析的四级深度

### 等级1：基础灵敏度（及格，80%的论文）
- 选3-5个参数，±10%、±20%
- 报告"影响不大/影响显著"
- **扣分原因**：没有揭示任何新信息

### 等级2：阈值发现（良好，15%的论文）
- 发现参数存在临界值（如碳税65元/吨）
- 解释临界值的存在原因
- 给出管理含义
- 示例："碳税从50→65元/吨，总成本几乎不变(企业可内部消化)；超过65元后成本急剧上升(每增加1元碳税，总成本增0.8%)。65元是政策的'有效门槛'"

### 等级3：交互效应（优秀，4%的论文）
- 发现两个参数之间的放大/抵消关系
- 用热力图/等高线图展示
- 示例："高碳税(>70元)+低速度(<30km/h)条件下，新能源车突然变得经济——碳税每增加10元，新能源车的经济优势扩大3.2%。但在高速条件下(>50km/h)，碳税对车型选择几乎无影响"

### 等级4：鲁棒优化建议（卓越，1%的论文）
- 基于灵敏度分析给出鲁棒参数设置
- 区分"可调的"和"结构性的"参数
- 示例："车辆固定成本不可调(由市场决定)，但路径规划可以通过增加每辆车的服务客户数来分摊固定成本。建议：在需求低谷期，将车辆利用率从当前的67%提升至85%以上，可抵消碳税上涨的影响"

## 输出每种等级的模板、示例、适用条件`,
    { label: '灵敏度模式', phase: '模式提取' }
  ),

  // 4. 算法匹配（基于算法指南）
  () => agent(
    `你是数学建模算法选型专家。请基于国赛优秀论文和算法指南，构建**问题类型→算法匹配表**。

## 参考算法指南知识
- 评价类：AHP层次分析法、TOPSIS、熵权法、模糊综合评价、灰色关联度、DEA数据包络
- 预测类：Logistic、灰色GM(1,1)、ARIMA/SARIMA、BP神经网络、LSTM、随机森林、高斯过程回归
- 优化类：线性/整数/非线性规划、粒子群PSO、遗传算法GA、模拟退火、蚁群算法、NSGA-II
- 创新算法组合：改进AHP-直觉模糊融合、动态权重TOPSIS、ARIMA+LSTM+XGBoost组合预测、PSO-GA自适应、DQN深度强化学习

## 请构建多维匹配表

### 维度1：基础匹配（问题类型 → 标准算法）
| 问题类型 | 子类型 | 标准算法 | 备选算法 | 论文中如何描述 |
|---------|--------|---------|---------|-------------|
| 评价类 | 指标定权 | AHP+熵权法组合赋权 | CRITIC法、变异系数法 | "综合主观经验与客观数据" |
| 预测类 | 线性时序 | ARIMA/SARIMA | 指数平滑、灰色GM(1,1) | "捕捉时间序列的自回归结构" |
| ... | ... | ... | ... | ... |

### 维度2：进阶匹配（问题特征 → 创新组合）
| 问题特征 | 为什么标准方法不够 | 创新组合方案 | 论文中的创新描述 |
|---------|------------------|------------|---------------|
| 数据含异常值+非线性 | 单一模型鲁棒性差 | ARIMA+LSTM+XGBoost加权组合 | "融合线性趋势+非线性模式+残差修正" |
| 多目标冲突 | 单目标无法刻画权衡 | NSGA-II+TOPSIS优选 | "Pareto前沿+多属性决策优选" |
| ... | ... | ... | ... |

### 维度3：论文包装（技术术语 → 学术表达）
| 技术术语 | 论文中的学术表达 | 为什么这样写更好 |
|---------|---------------|---------------|
| "用了遗传算法" | "采用自适应遗传算法，设计了问题特定的交叉和变异算子" | 展示算法适配性而非简单套用 |
| "调参得到" | "通过正交试验设计确定关键参数，平衡了解质量和计算效率" | 参数选择有方法论支撑 |
| ... | ... | ... |

## 输出完整匹配表（至少覆盖5种问题类型×3种子类型）`,
    { label: '算法匹配', phase: '模式提取' }
  ),

  // 5. 创新声明模式
  () => agent(
    `你是数学建模论文写作专家。请分析获奖论文中**创新点声明**的写作模式。

## 背景
国赛论文最常见的扣分项之一是"创新点夸大"——声称"提出了新算法"但实际只是标准方法。
获奖论文的创新声明有明确的分层模式。

## 请提取：创新声明的诚实分级

### 等级A：方法原创（极少数，需要真正的算法创新）
"本文提出了XX算法，其核心思想是..."
适用条件：确实提出了文献中不存在的新算法
风险：如果评委发现有类似算法 = 严重扣分

### 等级B：问题适配（最常见，最诚实）
"本文将XX算法首次应用于YY问题，并设计了针对YY问题特征的ZZ算子..."
适用条件：算法本身是现有的，但针对问题做了定制化改进
→ 这是最推荐的创新声明级别——诚实且展示深度

### 等级C：组合创新（常见，需要说清为什么组合）
"本文将A方法与B方法融合，利用A处理XX，利用B处理YY，形成了AB混合求解框架..."
适用条件：单一方法无法解决问题，需要组合
→ 必须说明"为什么A和B缺一不可"

### 等级D：改进创新（常见，需要量化改进效果）
"本文在标准XX算法的基础上，改进了其YY步骤，使ZZ指标提升了XX%..."
适用条件：对现有方法做了可量化的改进
→ 必须有对比数据证明改进有效

## 对每种等级输出：
1. **模板句式**（可直接使用的学术表达）
2. **使用条件**（什么情况下用这个等级）
3. **证据要求**（需要什么数据/对比来支撑这个声明）
4. **反面示例**（同等级的扣分写法）

## 创新声明的5条黄金法则
1. ...
2. ...`,
    { label: '创新声明模式', phase: '模式提取' }
  ),
])

// ===== 第二阶段：生成注入方案 =====
log('正在生成模式注入方案...')

const injectionPlan = await agent(
  `请基于以下提取的模式，生成可注入到 paper-generate.js 各阶段 prompt 中的**增强文本**。

## 摘要模式
${abstractPatterns.substring(0, 2000)}

## 公式解释模式
${formulaPatterns.substring(0, 2000)}

## 灵敏度分析模式
${sensitivityPatterns.substring(0, 2000)}

## 算法匹配
${algorithmMatch.substring(0, 2000)}

## 创新声明模式
${innovationPatterns.substring(0, 2000)}

## DOCX转换友好规则（重要！防止生成论文转Word时出现{}$%等残留符号）
在生成LaTeX论文时，应遵循以下规则以确保后续DOCX转换无残留：

### 必须遵守的规则
1. **行内数学模式**：尽量用 \\(...\\) 替代 $...$，减少$符号歧义
2. **特殊字符**：正文中的 %、&、#、_ 必须用 \\%、\\&、\\#、\\_ 转义
3. **避免孤立的 { }**：在正文段落中避免使用裸露的花括号（如 {example}），应改用 \\textit{example} 或其他LaTeX命令
4. **下标上标**：在正文中描述变量时尽量用文字（如"R_se"而非"R_{se}"），公式中正常使用
5. **嵌套括号**：避免深度嵌套的 {{{{...}}}}，
6. **表格环境**：只使用 tabular，避免嵌套在复杂的自定义环境中
7. **\\begin/\\end 配对**：确保所有环境标签严格配对，不遗漏

### 常见问题上限
- 论文中 $ 符号数量（应在数学模式内）：建议 < 50处
- 论文中裸露 { } 数量（不含LaTeX命令）：应为 0
- \\begin{xxx} 和 \\end{xxx} 数量差：应为 0

## 注入方案要求

### 注入点1：paper-generate.js → "摘要" agent prompt
在现有要求后追加：
\`\`\`
## 获奖摘要参考模式（来自国赛优秀论文分析）
[从摘要模式中提取的关键框架和铁律]
\`\`\`

### 注入点2：paper-generate.js → "五、模型" agent prompt
在公式解释要求后追加：
\`\`\`
## 公式解释深度参考（四级金字塔）
[从公式解释模式中提取的升级模板]
\`\`\`

### 注入点3：paper-generate.js → "六、评价" agent prompt
在灵敏度分析要求后追加：
\`\`\`
## 灵敏度分析深度参考（四级深度）
[从灵敏度分析模式中提取的深度示例]
\`\`\`

### 注入点4：algorithm-research.js → agent prompt
追加：
\`\`\`
## 算法选型参考（来自国赛算法指南）
[从算法匹配中提取的匹配表]
\`\`\`

### 注入点5：paper-generate.js → 创新相关agent prompt
追加：
\`\`\`
## 创新声明参考（诚实分级）
[从创新声明模式中提取的等级模板]
\`\`\`

## 输出格式
对每个注入点，输出**可直接追加到prompt中的完整文本**（不要省略、不要"参见上文"）。`,
  { label: '生成注入方案', phase: '模式提取' }
)

log('范文模式库分析完成！')
log('')

return {
  patterns: {
    abstract: abstractPatterns,
    formula: formulaPatterns,
    sensitivity: sensitivityPatterns,
    algorithmMatch,
    innovation: innovationPatterns,
  },
  injectionPlan,
  usage: `
将 injectionPlan 中每个注入点的文本追加到对应工作流的 agent prompt 中。
或在运行 paper-generate 时传入：
  Workflow({name:"paper-generate", args:{
    code:"main.py",
    problem:"题目.txt",
    patternInjection: injectionPlan  // 模式注入
  }})
  `,
}
