export const meta = {
  name: 'paper-generate',
  description: '论文生成v5：问题分类+自适应章节结构+叙事主线+三层公式解释+管理启示+摘要3版优选',
  phases: [
    { title: '问题分类', detail: '识别问题类型，调整论文策略' },
    { title: '信息提取', detail: '从代码和结果中提取模型信息' },
    { title: '确定主线', detail: '确定论文核心叙事主线+自适应章节计划' },
    { title: '并行生成', detail: '并行生成摘要及各章节（围绕主线+自适应结构）' },
    { title: '组装论文', detail: '按模板组装完整LaTeX文档' },
    { title: '摘要优选', detail: '生成3版摘要，评委打分选最优' },
    { title: '格式检查', detail: '逐项检查LaTeX语法、格式规范、公式完整性' },
  ],
}

// 论文生成工作流 v5
// v5 新增:
//   - 自适应章节计划（根据问题类型调整篇幅分配和子节结构）
//   - 摘要3版本生成 + 评委打分选最优
// v4 新增:
//   - 问题类型自动识别 → 调整论文策略
//   - 叙事主线设计 → 各章节围绕主线展开
//   - 三层公式解释要求（符号层+物理层+优化含义层）
//   - 管理启示子节（6.5）
//   - 基线对比要求
// 支持模板选择 + 增量更新
//
// 参数:
//   args.template: "huazhong-cup"(默认) | "cumcm" | "mcm-icm"
//   args.mode: "full"(默认) | "incremental"
//   args.updateSections: ["abstract","ch5","ch6_7"] (incremental模式)
//   args.code / args.results / args.problem: 文件路径

const argsObj = (typeof args === 'string') ? { code: args } : (args || {})
const files = {
  code: argsObj.code || 'main.py',
  results: argsObj.results || '.',
  problem: argsObj.problem || '题目.txt',
}
const template = argsObj.template || 'huazhong-cup'
const mode = argsObj.mode || 'full'
const updateSections = argsObj.updateSections || []
const lang = argsObj.lang || (template === 'mcm-icm' ? 'en' : 'zh')

// 模板配置
const tpl = {
  'huazhong-cup': { name: '华中杯', tex: '.claude/templates/paper-template.tex', lang: 'zh' },
  'cumcm': { name: '国赛(CUMCM)', tex: '.claude/templates/cumcm.tex', lang: 'zh' },
  'mcm-icm': { name: '美赛(MCM/ICM)', tex: '.claude/templates/mcm-icm.tex', lang: 'en' },
}[template] || { name: '华中杯', tex: '.claude/templates/paper-template.tex', lang: 'zh' }

log(`模板: ${tpl.name} | 模式: ${mode} | 语言: ${tpl.lang}`)
if (mode === 'incremental') {
  log(`增量更新: ${updateSections.join(', ')}`)
}

// ==========================================
// 新增：问题类型识别（v4 核心特性）
// ==========================================
phase('问题分类')
log('正在识别问题类型，调整论文策略...')

const problemTypeAnalysis = await agent(
  `请阅读题目文件"${files.problem}"，判断本题属于哪种数学建模问题类型，并给出论文策略建议。

## 问题类型分类（选最匹配的1个主类型+可选1个副类型）

### 类型A：优化类
特征：有明确的目标函数和约束条件，需要在可行域内寻找最优解
关键词：最小化/最大化、最优方案、调度、路径规划、资源配置
论文重点：约束完整性 > 算法创新性 > 最优性分析
最易忽略：最优性证明/下界分析、与精确解的差距估计

### 类型B：预测/拟合类
特征：基于历史数据预测未来趋势或缺失值
关键词：预测、拟合、回归、分类、趋势分析
论文重点：特征工程 > 模型对比 > 过拟合检验
最易忽略：残差分析、预测区间（非点预测）、过拟合检测

### 类型C：评价/决策类
特征：构建指标体系，对多个方案/对象进行评价排序
关键词：评价、评估、指标体系、权重、排序、等级
论文重点：指标合理性 > 权重科学性 > 灵敏度分析
最易忽略：指标相关性、权重主观性讨论、不同评价方法的对比

### 类型D：机理/物理建模类
特征：基于物理/工程原理建立微分方程或机理模型
关键词：微分方程、物理模型、机理、动力学、传递函数
论文重点：公式推导 > 参数辨识 > 实验验证
最易忽略：参数可辨识性、模型阶次选择、与数据驱动方法的对比

### 类型E：综合类（多子问题不同类型）
特征：一个大题包含多个子问题，各子问题类型不同
论文重点：逻辑衔接 > 各子问题方法互补性 > 整体框架

## 输出格式
### 主类型：[A/B/C/D/E]
### 副类型（如有）：[A/B/C/D/E]
### 论文篇幅分配建议
- 摘要：X%
- 问题重述+分析：X%
- 模型假设+符号：X%
- 模型建立与求解：X%（核心，建议>50%）
- 结果分析+验证：X%
- 评价+推广：X%

### 该类型论文的3个得分关键点
1. ...
2. ...
3. ...

### 该类型论文最常犯的3个错误
1. ...
2. ...
3. ...

### 建议重点突出的内容
（这个信息将用于指导后续所有章节的生成）`,
  { label: '识别问题类型', phase: '问题分类' }
)

log(`问题类型识别完成，论文策略已调整`)

phase('信息提取')
log('正在从代码和结果中提取模型信息...')

// 增量模式下只提取必要信息
let modelExtract, resultsExtract, problemSummary

if (mode === 'incremental' && updateSections.length > 0) {
  // 轻量提取
  [modelExtract, resultsExtract, problemSummary] = await parallel([
    () => agent(
      `快速浏览代码文件 ${files.code}，提取核心模型结构（目标函数+主要约束+关键参数）。用LaTeX公式格式。同时：如果代码中有简单的基线方法（如贪心/直接计算），一并提取用于对比。`,
      { label: '快速提取模型', phase: '信息提取' }
    ),
    () => agent(
      `快速浏览结果文件 ${files.results}，提取最新数值结果和关键发现。特别关注：与基线方法的对比数据（如有）、意外或反直觉的发现。`,
      { label: '快速提取结果', phase: '信息提取' }
    ),
    () => agent(`简述题目: ${files.problem}`, { label: '题目摘要', phase: '信息提取' }),
  ])
} else {
  // 完整提取
  ;[modelExtract, resultsExtract, problemSummary] = await parallel([
    () => agent(
      `请仔细阅读以下建模代码，提取用于撰写论文的关键信息：

主要代码文件: ${files.code}

## 问题类型背景
${problemTypeAnalysis}

请提取：

1. **数学模型**：
   - 目标函数：用严格的LaTeX数学公式写出
   - 所有约束条件：每条用LaTeX公式写出并编号 \\label
   - 决策变量：含义和类型（0-1/连续/整数）
   - 参数：含义、单位、取值

2. **算法描述**：
   - 算法名称和类型
   - 关键步骤流程（初始解→迭代→破坏→修复→接受→降温→权重更新）
   - 破坏算子/修复算子的数量和设计
   - 关键参数（初始温度、冷却率、迭代次数等）
   - **与简单基线方法的对比**：如果有实现简单贪心/规则方法，一并提取结果

3. **模型假设**：代码中隐含的假设和简化，每条假设的实际依据

4. **关键物理/工程公式**：能耗计算、约束检查、成本计算等

请用LaTeX数学公式格式输出所有数学表达式。`,
    { label: '提取模型信息', phase: '信息提取' }
  ),
    () => agent(
      `请读取以下结果文件，提取用于论文的关键数据：

结果路径: ${files.results || '.'}

## 问题类型背景
${problemTypeAnalysis}

请提取：
1. **主要数值结果**：目标函数值及分解(车辆/能耗/惩罚/碳成本)、车辆数、各车型数等
2. **基线对比数据**：如果有与简单方法（贪心/规则/heuristic）的对比，重点提取
3. **图表清单**：已有图表的路径和内容描述
4. **对比分析**：各子问题的对比数据、灵敏度分析结果
5. **关键发现**：最重要的3-5个发现及其含义
6. **意外/反直觉的发现**：（这类发现往往成为论文的亮点）
7. **管理/工程启示**：从结果中可以得出什么实际建议？

用表格和具体数字呈现。`,
    { label: '提取结果信息', phase: '信息提取' }
  ),
    () => agent(
      `请阅读题目文件并生成论文所需的题目摘要：

题目文件: ${files.problem || '题目.txt'}

## 问题类型背景
${problemTypeAnalysis}

请输出：
1. **问题一句话概述**（15-25字）
2. **核心难点**（3-5个）
3. **问题背景段落**（可用于1.1问题背景）
4. **3个子问题简述**（逐个说明求解目标）`,
    { label: '题目摘要', phase: '信息提取' }
  ),
])
}

// ==========================================
// 新增：确定叙事主线（v4 核心特性）
// ==========================================
phase('确定主线')
log('正在确定论文核心叙事主线...')

const narrativeThread = await agent(
  `你是一位资深学术论文写作者。请阅读以下信息，确定这篇论文的核心叙事主线。

## 题目信息
${problemSummary}

## 问题类型与策略
${problemTypeAnalysis}

## 模型信息
${modelExtract.substring(0, 3000)}

## 结果信息
${resultsExtract.substring(0, 2000)}

## 叙事主线设计

好的论文有一条贯穿始终的"故事线"。请设计这篇论文的叙事主线：

### 1. 核心论点（一句话，15-25字）
格式："这个问题的本质是___，传统方法难以___，本文通过___解决，关键发现是___"

### 2. 叙事逻辑链（3-5步）
逻辑关系：现象 → 本质 → 方法 → 验证 → 启示
例如：
- 步骤1（现象）：表面看是车辆调度问题
- 步骤2（本质）：实质是多目标约束下的大规模组合优化
- 步骤3（方法）：通过ALNS+自适应机制突破求解瓶颈
- 步骤4（验证）：与贪心基线对比提升23%，灵敏度分析揭示碳税阈值效应
- 步骤5（启示）：碳税65元/吨是政策关键拐点，建议分阶段实施

### 3. 每个章节的叙事任务
- 摘要：浓缩步骤1-5，给评委30秒内建立认知
- 一、问题重述：展开步骤1-2，建立问题的重要性
- 二、问题分析：深化步骤2，展示对问题本质的洞察
- 三、模型假设：为步骤3做铺垫，说明简化的合理性
- 四、符号说明：为步骤3提供"工具箱"
- 五、模型建立与求解：步骤3的主体，展示技术深度
- 六、评价与优化：步骤4+5，验证+启示
- 七、推广：步骤5的延展

### 4. 论文的记忆点设计
评委看完论文24小时后应该记住什么？（1句话）
例如："碳税65元是临界点，超过后企业行为发生质变"

### 5. 摘要的"黄金30秒"脚本
如果评委只有30秒看摘要，他最应该看到的一串信息是什么？
（按顺序列出3-4个关键数字+发现）

请输出以上内容。这个主线将注入到后续所有章节的生成中，确保全文逻辑一致。`,
  { label: '确定叙事主线', phase: '确定主线' }
)

log('叙事主线已确定，各章节将围绕主线展开')

// ==========================================
// 新增：自适应章节计划（v5 — 根据问题类型调整章节结构）
// ==========================================
log('正在根据问题类型生成自适应章节计划...')

const chapterPlan = await agent(
  `基于以下信息，为论文生成**自适应章节计划**。不同问题类型需要不同的章节侧重点。

## 问题类型分析
${problemTypeAnalysis}

## 叙事主线
${narrativeThread}

## 模型信息
${modelExtract.substring(0, 2000)}

## 结果信息
${resultsExtract.substring(0, 1000)}

## 自适应章节计划要求

### 基准结构（七章制）
一、问题重述 → 二、问题分析 → 三、模型假设 → 四、参数与变量说明 → 五、模型的建立与求解 → 六、模型的评价与优化 → 七、模型的应用与推广

### 按问题类型调整篇幅分配

#### 优化类问题
- 五、模型建立与求解：60%（强化约束完整性+算法创新）
- 增加子节"5.2.1.X 最优性分析"（与精确解的差距估计）
- 六、评价与优化：25%（强化基线对比+灵敏度）
- 其余15%

#### 预测/拟合类问题
- 五、模型建立与求解：45%（强化特征工程+模型对比）
- 增加子节"5.1.3 特征工程"为独立子节（非subsubsection）
- 增加子节"5.2.1.X 过拟合分析与防控"
- 六、评价与优化：30%（强化过拟合检测+残差分析+预测区间）
- 其余25%

#### 评价/决策类问题
- 五、模型建立与求解：40%（强化指标体系构建+权重设计）
- 增加子节"5.1.3 指标体系构建"为独立子节
- 增加子节"5.2.1.X 权重合理性讨论"
- 六、评价与优化：30%（强化权重灵敏度+排序稳定性）
- 其余30%

#### 机理/物理建模类问题
- 五、模型建立与求解：50%（强化公式推导+参数辨识）
- 增加子节"5.2.1.X 模型阶次/结构选择"
- 增加子节"5.2.1.Y 参数可辨识性分析"
- 六、评价与优化：25%（强化实验验证+与数据驱动方法对比）
- 其余25%

## 输出格式
### 问题类型：XX
### 建议篇幅分配
| 章节 | 建议篇幅占比 | 与基准的调整 | 理由 |
|------|------------|------------|------|
| 一 | X% | +/-X% | ... |
| ... | ... | ... | ... |

### 建议新增的子节（如有）
1. [子节编号] [标题] — [放在哪个章节] — [原因]

### 建议缩减/合并的子节（如有）
1. [子节编号] — [缩减方案]

### 该问题类型最容易遗漏的3个内容（务必在对应章节体现）
1. ...
2. ...
3. ...

### 注入各章节agent的关键指令
- 摘要agent额外指令：
- 五、模型agent额外指令：
- 六、评价agent额外指令：`,
  { label: '自适应章节计划', phase: '确定主线' }
)

log('自适应章节计划已生成')

phase('并行生成')
log('正在围绕叙事主线+自适应章节计划并行生成论文全部章节...')

// 并行生成全部9个章节（每个agent都收到叙事主线 + 章节计划）
const [
  abstractCh,
  ch1Restatement,
  ch2Analysis,
  ch3Assumptions,
  ch4Notation,
  ch5Model,
  ch6Evaluation,
  appendixCh,
  referencesCh,
] = await parallel([
  // ---- 摘要 ----
  () => agent(
    `撰写论文**摘要**部分（300-500字）。

## 叙事主线（摘要必须忠实遵循此主线）
${narrativeThread}

## 自适应章节计划（了解整篇论文的重点方向）
${chapterPlan.substring(0, 1000)}

背景: ${problemSummary}
模型: ${modelExtract.substring(0, 2000)}
结果: ${resultsExtract.substring(0, 1500)}

## 格式要求
1. 结构：问题本质(1句)→方法(1-2句)→模型与算法(2-3句)→关键结果含数字(2-3句)→创新贡献+管理启示(1句)
2. **关键数字必须保留**（这是评委第一眼寻找的东西，没有数字的摘要 = 致命缺陷）
3. 避免"本文"超过3次，换用"本研究""本工作""我们"等
4. 无引用编号、无公式编号、无图表编号
5. 最后一句话应让评委感受到"这个工作有实际价值"

输出：
- 第一行：摘要正文（纯文本，300-500字）
- 最后一行：关键词（3-5个，分号分隔）`,
    { label: '摘要', phase: '并行生成' }
  ),
  // ---- 一、问题重述 ----
  () => agent(
    `撰写论文**一、问题重述**（约600-800字）。

## 叙事主线（本章的叙事任务是"展开现象→揭示本质"）
${narrativeThread}

题目: ${problemSummary}
原文: ${files.problem || '题目.txt'}

子节结构：
### 1.1 问题背景（约300-400字）
- 行业背景（联系实际，不能泛泛谈"随着社会发展"——要具体到数字、政策、事件）
- 现实意义和研究价值（如果不能回答"谁关心这个问题？为什么现在关心？"就不够好）

### 1.2 问题重述（约400-500字）
- 学术语言重述，不照抄原文
- 已知条件：数据结构、参数范围、约束条件
- 3个问题逐问说明求解目标
- **保留原题关键数字**，这些数字是模型参数的来源

输出LaTeX格式(含\\subsection)。`,
    { label: '一-问题重述', phase: '并行生成' }
  ),
  // ---- 二、问题分析 ----
  () => agent(
    `撰写论文**二、问题分析**（约600-900字）。

## 叙事主线（本章的叙事任务是"深化对问题本质的洞察"）
${narrativeThread}

题目: ${problemSummary}
模型: ${modelExtract.substring(0, 2000)}

子节结构：
### 2.1 问题1分析（约200-300字）
- 问题类型识别
- 核心难点(3-5个)及原因（不能只说"这是NP-hard问题"，要说明为什么NP-hard在此处特别麻烦）
- 解题思路框架（图文配合，建议用简短的"思路→方法→工具"链条）

### 2.2 问题2分析（约200-300字）
- 与问题1的差异（具体到新增了哪个参数、哪个约束）
- 新增约束的影响（对计算量、对解的结构的影响）
- 解题策略调整

### 2.3 问题3分析（约200-300字）
- 与前两个问题的本质区别
- 新增挑战

输出LaTeX格式(含\\subsection和\\subsubsection)。`,
    { label: '二-问题分析', phase: '并行生成' }
  ),
  // ---- 三、模型假设 ----
  () => agent(
    `撰写**三、模型假设**（6-8条）。

## 叙事主线（本章的叙事任务是"为建模做铺垫，说明简化的合理性"）
${narrativeThread}

模型信息: ${modelExtract.substring(0, 3000)}

## 要求（升级版——每条假设必须有理由）
1. 使用 \\begin{enumerate}[label=\\arabic*.] 格式
2. 每条假设的格式：「假设内容。**合理性说明**：...（1-2句说明为什么可以这样假设）」
3. 涵盖：路径闭合、容量不超载、拆分配送、时变速度、能耗修正等
4. **假设不能过度简化**（如"假设所有车辆匀速行驶"对于有时变速度的题目就是过度简化）
5. 每条假设必须是"真假设"（确实简化了问题），不能是"废话假设"（如"假设数据真实可靠"）

## 反面示例（以下类型的假设会被评委扣分）
❌ "假设所有数据真实有效" — 这不是建模假设，是废话
❌ "假设不考虑极端情况" — 太模糊，"极端"是什么？
✅ "假设车辆在配送中心出发后直接前往首个客户，忽略出库装车时间（因为装车时间<总配送时间的2%）"

输出LaTeX enumerate列表。`,
    { label: '三-模型假设', phase: '并行生成' }
  ),
  // ---- 四、参数与变量说明 ----
  () => agent(
    `撰写**四、参数与变量说明**（两个三线表）。

## 叙事主线（本章的叙事任务是"为读者提供理解模型所需的'工具箱'"）
${narrativeThread}

模型信息: ${modelExtract.substring(0, 3000)}

要求：
### 参数表（15-25个参数）
\\begin{table}[H] \\centering \\caption{模型参数}
列：符号 | 含义 | 单位 | 取值/来源
三线表(\\toprule \\midrule \\bottomrule)
新增"取值/来源"列，说明参数值是从哪里来的（题目给定/文献引用/实际估算）

### 决策变量表
\\begin{table}[H] \\centering \\caption{决策变量}
列：符号 | 含义 | 类型 | 取值范围

符号规范：集合用大写(K,N)，参数用小写+下标，变量用x,y,s
类型标注：0-1变量 / 连续变量 / 整数变量

输出LaTeX格式。`,
    { label: '四-符号说明', phase: '并行生成' }
  ),
  // ---- 五、模型的建立与求解 ----
  () => agent(
    `撰写核心章节**五、模型的建立与求解**（约3000-4000字）。

## 叙事主线（本章的叙事任务是"展示技术深度的主体部分"）
${narrativeThread}

## 问题类型策略
${problemTypeAnalysis}

模型: ${modelExtract}
结果: ${resultsExtract.substring(0, 2000)}

## 结构

### 5.1 数据预处理
#### 5.1.1 数据概况与特征分析
- 数据概况(客户数、需求量等)
- 引用图5-1数据分布图
- 关键统计数字

#### 5.1.2 数据清洗与特征工程
- 缺失值/异常值处理及理由
- 特征构造及理由

#### 5.1.3 [如有]速度时段/能耗等预处理建模
- 时段划分、参数设定
- 公式和参数表

### 5.2 问题1的模型与求解
#### 5.2.1 模型建立

**建模思路引入段**（必须先有文字引导，再出现公式）

**目标函数**（使用 \\begin{equation}）：
- 完整公式
- **三层解释（这是 v4 核心升级！）**：
  (1) **符号层**：每个符号的数学含义（"C_vehicle = Σ c_k·y_0k，表示各车辆固定启动成本之和"）
  (2) **物理/经济层**：每项成本在实际中对应什么（"这反映了每多派一辆车就产生固定费用（司机、折旧）"）
  (3) **优化含义层**：这项成本如何影响模型的决策倾向（"因此模型会倾向于用尽量少的车、尽量装满每辆车去服务客户"）

**约束条件**（6条以上，使用 \\begin{align} 环境）：
- 每条 \\label{eq:xxx}
- 每条约束后必须跟**四段式详细解释**：
  > 约束(\\ref{eq:xxx})确保/要求...[实际含义]。
  > 为什么要这个约束？如果不满足会导致...[具体后果]。
  > 这条约束是否可能过于严格或过于宽松？[合理性讨论]

#### 5.2.2 求解算法
- **算法选择理由**：问题规模分析 + 为什么不用精确算法 + 为什么选这个启发式
- algorithm伪代码(\\REQUIRE/\\ENSURE)
- 初始解生成策略详述
- 破坏算子(逐个说明设计动机和效果)
- 修复算子(逐个说明设计动机和效果)
- 自适应权重机制(得分规则+设计理由)
- 接受准则(Metropolis)
- 参数设置（每个参数都要有选择理由）

#### 5.2.3 求解结果
- **基线对比**（v4新增，必须有！）：
  "为验证本文算法有效性，与以下基线方法对比：
  (1) 贪心算法：按最近邻原则构造路径，总成本XXX
  (2) [如适用] Gurobi精确解（小规模）：总成本XXX
  本文ALNS算法得到总成本XXX，较贪心降低XX%，与精确解差距仅XX%"
- 结果汇总表(三线表)
- 图表引用和分析（**三层描述法——禁止重复！**）：
  (1) **图题（\\caption）**：≤15字简短标签，只说明"什么情况下什么图"（如"图5-1 情况一月球绕日运动轨迹"），不含分析
  (2) **引导句（图前1句）**：说明"为展示XX，图5-1呈现了YY"，不与图题重复
  (3) **解读句（图后1-3句）**：分析图中数据的含义、原因和启示（这是核心，不能只用一句"如图5-1所示"跳过）
  ❌ 反面示例（三重重复扣分）：
    \\caption{图5-1 情况一圆形共面轨道仿真结果}
    正文："图5-1展示了情况一的仿真结果。如图5-1所示，情况一的圆形共面轨道仿真结果..."
  ✅ 正确示例：
    \\caption{图5-1 月球绕日运动轨迹（情况一：圆形共面）}
    正文："为直观展示情况一的轨迹特征，图5-1给出了月球在1年内的运动路径。轨迹呈内摆线形态，每年形成12.37个振幅周期，与地月公转周期比ω_m/ω_e≈13.37一致..."
- **结果解读要有深度**：不只是"表5-1显示XX"，而是"表5-1显示XX，这说明YY，可能的原因是ZZ"

### 5.3 问题2（同上结构，约800-1000字）
- 重点说明与问题1的差异和策略调整

### 5.4 问题3（同上结构，约800-1000字）
- 重点说明与前两个问题的本质区别

## 格式规范
1. 公式前后必须都有文字衔接（引导句+逐层解释）
2. 表格使用三线表
3. **【强制】每个公式必须有 \\label{eq:xxx}，每个 \\ref{eq:xxx} 必须对应一个已存在的 \\label**。
   - ❌ 禁止出现"公式()"或"如公式所示"等编号缺失的引用
   - ✅ 正确格式：先定义 \\begin{equation}\\label{eq:radial}...\\end{equation}，再引用"由公式(\\ref{eq:radial})可知"
   - 组装论文后检查：\\label 数量和 \\ref 数量是否匹配？每个 \\ref 是否都有对应的 \\label？
4. 图表引用：如图\\ref{fig:xxx}所示/如表\\ref{tab:xxx}所示
5. **禁止连续3个以上公式无文字衔接**

输出LaTeX格式(含\\subsection和\\subsubsection)。`,
    { label: '五-模型建立与求解', phase: '并行生成' }
  ),
  // ---- 六+七 ----
  () => agent(
    `撰写**六、模型的评价与优化**和**七、模型的应用与推广**。

## 叙事主线（本章的叙事任务是"验证可信度→挖掘启示→展望未来"）
${narrativeThread}

## 问题类型策略
${problemTypeAnalysis}

模型: ${modelExtract.substring(0, 2000)}
结果: ${resultsExtract.substring(0, 2000)}

### 六、模型的评价与优化
#### 6.1 验证方法（200-300字）
- 约束满足度检查
- 小规模实例与精确解/Gurobi对比验证
- [预测类] 训练/验证/测试集性能对比

#### 6.2 敏感性分析（400-500字，深度升级）
- 关键参数灵敏度表格/热力图
- **不只是"影响不大"，要挖掘非平凡关系**：
  - 是否存在阈值效应？（参数超过某值后结果突变）
  - 是否存在非单调关系？
  - 哪些参数之间有交互效应？
- 管理启示（每条发现对应一个实际建议）

#### 6.3 模型优点（4-5条）
\\begin{enumerate}...\\end{enumerate}
- **每条优点必须配具体数据或对比**（❌"模型具有较好的鲁棒性" → ✅"在±20%参数扰动下，目标函数变异系数仅4.2%，表明模型具有较好的鲁棒性"）
- 包含至少1条与基线方法对比的优势

#### 6.4 模型缺点（3-4条）
- **诚实且具体**（❌"本模型假设较强" → ✅"本模型假设速度分布为已知的静态分布，实际交通状况存在随机波动。在小规模实例上，忽略随机性使成本低估约2-5%；在大规模实例上预计低估更多"）
- 每条缺点附带影响评估和可能的改进方向

#### 6.5 管理启示与政策建议（v4新增，200-300字）
基于模型结果，提出对实际管理或政策的建议：
- 每条建议必须有具体数字支撑（不是"应该优化配送路线"而是"优化配送路线可降低12%总成本，建议企业优先投资路线优化系统"）
- 区分"企业层面建议"和"政策层面建议"
- 如果涉及阈值/临界值，务必明确指出（如"碳税65元/吨是政策关键拐点"）

### 七、模型的应用与推广（300-400字）
- 其他应用场景(具体到行业和场景，不是泛泛的"可用于其他领域")
- 其他行业借鉴
- 需做的调整

输出LaTeX格式(含\\subsection和\\subsubsection)。`,
    { label: '六-七-评价推广', phase: '并行生成' }
  ),
  // ---- 附录 ----
  () => agent(
    `根据代码和结果生成**附录**内容：

## 叙事主线
${narrativeThread}

代码: ${files.code || 'main.py'}
结果: ${resultsExtract.substring(0, 1500)}

要求：
### 附录A/B：参数表/补充数据
### 附录C：核心求解代码
- 列出关键函数（主求解入口、核心算法、约束检查等）
- 使用 \\begin{verbatim}...\\end{verbatim}
- 只保留核心代码段（每个约20-40行）
- 代码注释用中文说明

输出LaTeX格式(\\section*{附录}+\\subsection*{附录X：标题})。`,
    { label: '附录', phase: '并行生成' }
  ),
  // ---- 参考文献 ----
  () => agent(
    `生成**参考文献**列表（10-15篇）。

## 叙事主线
${narrativeThread}

题目: ${problemSummary}
模型: ${modelExtract.substring(0, 2000)}

## 要求（升级版）
1. 使用 \\begin{thebibliography}{99}...\\end{thebibliography}
2. 每条 \\bibitem{refN} 格式
3. **必须包含以下类型**：
   - 该领域的经典奠基论文（1-2篇）
   - 核心算法的原始论文（1-2篇）
   - 中文核心期刊文献（2-3篇）
   - 近年高质量文献（3-5篇，2018年以后）
   - 相关教材/专著（1-2篇）
4. 格式：[N] 作者. 标题[J]. 期刊, 年份, 卷(期): 页码.
5. **中英文混合**，体现文献调研的广度
6. **不要编造文献**——只列出你有把握存在的文献。
7. **【强制】引用规范（v5新增）**：
   - 每个 \\cite{refN} 必须紧跟在被引用的**具体声明**之后，而非笼统放在段落末尾
   - ❌ 错误："月球运动经历了从解析到数值的演进[1][2]。"（两个引用堆在一起，无法区分各自支持什么）
   - ✅ 正确："开普勒在1609年提出行星运动三大定律[1]。Roughgarden等人将开普勒方程应用于地月系统数值仿真[2]。"
   - 禁止出现孤立的引用编号（如正文中"[1]"与前后文无逻辑关联）
   - **附录代码中的算法必须标注来源**：在关键函数前加注释 `# 算法来源：[N] 作者. 标题`
   - 代码中使用的数值方法（Newton-Raphson、Runge-Kutta等）必须在参考文献中有对应条目

输出LaTeX \\bibitem列表。`,
    { label: '参考文献', phase: '并行生成' }
  ),
])

phase('组装论文')
log('正在依照模板组装完整LaTeX论文...')

const latexPaper = await agent(
  `请严格按照 ${tpl.tex} 模板的结构（${tpl.name}格式），将以下内容组装成完整的LaTeX论文。

## 叙事主线（确保论文逻辑一致）
${narrativeThread}

## 格式规范（必须遵守）

### 页面与字体
- \\documentclass[12pt,letterpaper]{ctexart}
- 页边距: left=3.17cm, right=2.75cm, top=2.6cm, bottom=2.2cm
- 标题: {\\heiti\\zihao{3}\\textbf{论文标题}} 居中，三号黑体加粗
- "摘 要"标签: {\\heiti\\zihao{3}} 居中，三号黑体
- 摘要正文: {\\songti\\zihao{4}} 四号宋体，\\parindent=2em
- 关键词: {\\songti\\zihao{4}}，\\textbf{关键词：}加粗
- 正文: \\zihao{-4} 小四号(12pt)，\\parindent=2em，\\linespread{1.3}
- 一级标题(一、二...): {\\heiti\\zihao{4}\\centering} 四号黑体居中
- 二级标题(1.1,2.1...): {\\heiti\\zihao{-4}\\raggedright} 小四黑体左对齐
- 三级标题(5.1.1...): {\\heiti\\zihao{-4}\\raggedright} 小四黑体左对齐

### 页眉页脚
- 页眉: \\fancyhead[C]{\\songti\\zihao{5}\\leftmark} 居中五号宋体
- 页脚: \\fancyfoot[C]{\\thepage} 居中页码

### 图表
- 表编号: "表\\thesection-\\arabic{table}" (如表5-1)
- 图编号: "图\\thesection-\\arabic{figure}" (如图5-1)
- 表格: 三线表(\\toprule \\midrule \\bottomrule)，标题在表上方
- 图表标题: \\caption{表/图X-X 标题}
- 图表居中: \\centering

### 公式
- \\begin{equation}\\end{equation} 或 \\begin{align}\\end{align}
- 禁止 \\$\\$...\\$\\$
- 每个公式 \\label{eq:xxx}

### 结构
- 一级标题用中文数字: \\section{一、问题重述}...\\section{七、模型的应用与推广}
- 二级用阿拉伯: \\subsection{1.1 问题背景}
- 目录页在摘要后
- 参考文献用 \\begin{thebibliography}
- 附录用 \\section*{附录}+\\subsection*{附录X：标题}

---

## 各章节内容

### 论文标题
（根据叙事主线生成，15-25字学术化中文标题，包含核心关键词）

### 摘要与关键词
${abstractCh}

### 一、问题重述
${ch1Restatement}

### 二、问题分析
${ch2Analysis}

### 三、模型假设
${ch3Assumptions}

### 四、参数与变量说明
${ch4Notation}

### 五、模型的建立与求解
${ch5Model}

### 六、模型的评价与优化 / 七、模型的应用与推广
${ch6Evaluation}

### 参考文献
${referencesCh}

### 附录
${appendixCh}

---

请输出完整的 .tex 文件。确保 \\begin{} 和 \\end{} 全部配对，括号闭合，数学模式正确。
特别注意：模板中已有的 \\CTEXsetup 等格式设置**务必保留**，不要修改或删除。`,
  { label: '组装LaTeX论文', phase: '组装论文' }
)

// ==========================================
// 新增：摘要优选（v5 — 生成3版摘要，评委打分选最优）
// ==========================================
phase('摘要优选')
log('正在生成摘要的3个候选版本，由评委选择最优...')

const abstractVersions = await agent(
  `基于已生成的摘要，再创作**2个不同风格的备选版本**。注意：保留原文中的具体数据和关键发现。

## 原始版本（版本A——已生成的标准版）
${abstractCh}

## 叙事主线
${narrativeThread}

## 关键结果数据
${resultsExtract.substring(0, 1000)}

## 创作要求：生成版本B和版本C

### 版本B：数据密集型
风格特征：
- 以数字驱动，第一句话就出现具体数字
- 密集使用量化结果（如"总成本降低23.5%，碳排放减少18.2%，车辆利用率从67%提升至89%"）
- 用分号分隔多个定量发现
- 300-500字

### 版本C：故事驱动型
风格特征：
- 以问题洞察开场（如"XX问题的本质不是YY，而是ZZ"）
- 强调从问题→方法→发现→启示的完整叙事弧
- 最后一句话留给管理启示或更广泛的意义
- 300-500字

## 输出格式
### 版本B（数据密集型）
[摘要正文]
**关键词：**...

### 版本C（故事驱动型）
[摘要正文]
**关键词：**...`,
  { label: '生成备选摘要', phase: '摘要优选' }
)

const abstractSelection = await agent(
  `你是数学建模竞赛的评阅老师。请从以下3个摘要版本中选择最优的一个。

## 版本A（标准版）
${abstractCh}

## 版本B（数据密集型）& 版本C（故事驱动型）
${abstractVersions}

## 评分标准（每项1-5分）
1. **信息密度**：每句话是否有实质内容？是否有具体数字？
2. **叙事吸引力**：是否有清晰的"问题→方法→发现→启示"逻辑链？
3. **记忆点**：看完后评委能记住什么？
4. **学术规范**：是否符合300-500字、无引用、无口语化？

## 输出格式
### 版本对比
| 版本 | 信息密度 | 叙事吸引力 | 记忆点 | 学术规范 | 总分 |
|------|---------|-----------|--------|---------|------|
| A | X/5 | X/5 | X/5 | X/5 | XX/20 |
| B | X/5 | X/5 | X/5 | X/5 | XX/20 |
| C | X/5 | X/5 | X/5 | X/5 | XX/20 |

### 优胜版本：[A/B/C]
### 优胜理由：
### 是否建议混合？（如"B的数据密度 + C的叙事弧"）如果是，给出混合版的修改建议。`,
  { label: '评委选择最优摘要', phase: '摘要优选' }
)

log(`摘要优选完成，最优版本已选出`)
log('')

phase('格式检查')
log('正在逐项检查LaTeX格式规范合规性...')

const formatCheck = await agent(
  `请对以下LaTeX论文进行**格式规范逐项检查**（v4增强版）：

${latexPaper.substring(0, 10000)}

## 检查清单

### A. 文档设置
- [ ] \\documentclass[12pt,letterpaper]{ctexart}
- [ ] geometry: left=3.17cm, right=2.75cm, top=2.6cm, bottom=2.2cm

### B. 字体字号
- [ ] 标题: \\heiti\\zihao{3} 三号黑体居中
- [ ] "摘 要": \\heiti\\zihao{3} 三号黑体居中
- [ ] 摘要正文: \\songti\\zihao{4} 四号宋体
- [ ] 关键词: \\textbf{关键词：}加粗
- [ ] 一级标题(一~七): \\heiti\\zihao{4} 四号黑体居中
- [ ] 二级标题(1.1等): \\heiti\\zihao{-4} 小四黑体左对齐
- [ ] 正文: \\zihao{-4} 小四宋体

### C. 段落
- [ ] \\parindent=2em 首行缩进2字符
- [ ] \\linespread{1.3} 1.3倍行距

### D. 页眉页脚
- [ ] 页眉: \\leftmark 居中显示节名
- [ ] 页脚: \\thepage 居中页码
- [ ] \\headrulewidth=0.4pt

### E. 图表
- [ ] 表格三线表(\\toprule/\\midrule/\\bottomrule)
- [ ] 表编号格式: 表X-X
- [ ] 图编号格式: 图X-X
- [ ] 表格标题在表上方，居中
- [ ] 图表使用 \\centering

### F. 公式（v4增强检查）
- [ ] 无 \\$\\$...\\$\\$ (全部使用equation/align)
- [ ] 每个公式 \\label{eq:xxx}
- [ ] **每个公式前有引导句**（不是直接\\begin{equation}）
- [ ] **每个公式后有文字解释**（不只是"其中，x表示x"的符号翻译）
- [ ] **每条约束说明了"为什么需要"和"不满足会怎样"**
- [ ] **无连续3个以上公式无文字衔接**

### G. 结构完整性
- [ ] 七章完整(一至七)
- [ ] 目录页
- [ ] 参考文献(10-15条)
- [ ] 附录
- [ ] **6.5 管理启示与政策建议 子节（v4新增检查）**

### H. 引用格式
- [ ] 参考文献 [1][2] 格式
- [ ] bibitem 格式正确
- [ ] 中英文文献混合

### I. LaTeX语法
- [ ] \\begin{}-\\end{} 全部配对
- [ ] {} [] () 括号全部配对
- [ ] 特殊字符(& % # _)转义正确
- [ ] 数学模式 \$...\$ 闭合
- [ ] 无已废弃命令(\\bf \\rm等)

### J. 中文排版
- [ ] ctex 宏包
- [ ] 中文字体正确(\\songti \\heiti)
- [ ] 中文标点规范

### K. 叙事一致性（v4新增）
- [ ] 摘要中的关键数字是否在正文中出现？
- [ ] 摘要中的方法描述是否与第五章一致？
- [ ] 结论是否呼应了摘要的承诺？
- [ ] 符号系统是否全文一致（第三章定义的在第五章是否一致使用）？

### L. 公式编号完整性检查（v5新增）
- [ ] **每个 \\label{eq:xxx} 是否有对应的 \\ref{eq:xxx} 引用？**（定义了但没引用的=冗余）
- [ ] **每个 \\ref{eq:xxx} 是否有对应的 \\label{eq:xxx} 定义？**（引用了但没定义的=空白编号）
- [ ] 全文是否出现"公式()"或"公式（）"？（=编号缺失，必须修复）
- [ ] \\label 和 \\ref 的数量是否匹配？

### M. 图题与正文描述去重检查（v5新增）
- [ ] 同一张图的描述是否在正文中重复出现2次以上？
- [ ] 图题（\\caption）是否简洁（≤15字标题，不含分析解读）？
- [ ] 正文中是否区分了"引导句"（1句，说明图展示什么）和"解读句"（分析含义，不重复引导句）？
- [ ] ❌ 反面示例："图5-1展示了情况一的仿真结果"（引导句）+ "图5-1展示了情况一圆形共面轨道的仿真结果"（图题重复）= 扣分

### N. 参考文献引用规范检查（v5新增）
- [ ] 每个 \\cite{refN} 是否紧跟在被引用的具体声明之后（而非笼统放在段末）？
- [ ] 是否出现孤立的引用编号（如正文中单独出现"[1]"而无上下文关联）？
- [ ] 附录代码中是否标注了算法来源？（如 `# 算法来源：[N] 作者. 标题...`）
- [ ] 代码中引用的数值方法（如Newton-Raphson、Runge-Kutta）是否在参考文献中有对应条目？

对每项输出: ✅通过 / ⚠️需改进 / ❌缺失，附具体位置和修复代码。`,
  { label: '格式规范检查', phase: '格式检查' }
)

log('论文生成完成！')

return {
  problemType: problemTypeAnalysis,
  narrativeThread: narrativeThread,
  chapterPlan: chapterPlan,
  latexCode: latexPaper,
  abstractVersions: { a: abstractCh, b_and_c: abstractVersions, selection: abstractSelection },
  formatCheck: formatCheck,
  sections: {
    abstract: abstractCh,
    ch1Restatement, ch2Analysis, ch3Assumptions,
    ch4Notation, ch5Model, ch6Evaluation,
    referencesCh, appendixCh,
  },
}
